---
# Secret for MySQL root password used by the Deployment (todo-secret)
# Replace <base64-password> with the base64-encoded password value
apiVersion: v1
kind: Secret
metadata:
  name: todo-secret
type: Opaque
data:
  # key used in deployment: DB_PASSWORD
  DB_PASSWORD: <base64-password>

